const body = document.body;

function darkMode(){
  if (body.style.backgroundColor==='white'){
                            body.stylegroundColor==='black';
                            body.style.color'white';
  }else{
    body.style.bachgroundColor==='white';
    body.style.color==='black';
  }
}
